{
  "name": "@alphagate/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "biome check .",
    "test": "vitest run"
  },
  "dependencies": {
    "kysely": "^0.27.4"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}

