## Core: Database (Supabase Postgres)
# Full Postgres connection string to your Supabase DB.
DATABASE_URL=
# Supabase REST/admin access (optional, for supabase-js usage)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
## Supabase Types Generation (for Kysely bridge)
# Use either remote project or local DB:
# Remote: set both and run the CLI with --project-id
SUPABASE_ACCESS_TOKEN=
SUPABASE_PROJECT_ID=
# Local alternative: ensure DATABASE_URL points to a reachable DB and run the CLI with --local

## ClickHouse (Dev via Docker)
# For local Docker: http://localhost:8123, default user 'default', password '' unless configured
CLICKHOUSE_URL=
CLICKHOUSE_USER=
CLICKHOUSE_PASSWORD=
CLICKHOUSE_DATABASE=

## Portkey Gateway (OSS)
# Self-hosted Portkey Gateway base URL
PORTKEY_GATEWAY_URL=
# Org-scoped token used by the edge worker/main app to call the gateway
PORTKEY_ORG_TOKEN=

## AWS KMS (BYOK decryption for provider keys)
AWS_REGION=
AWS_KMS_KEY_ID=

## Better Auth (Dashboard/Auth)
# Public base URL of the app handling auth (e.g., http://localhost:3000)
AUTH_URL=http://localhost:3000
# 32+ byte random string for session/JWT signing (generate securely)
AUTH_SECRET=
# Optional cookie settings
AUTH_COOKIE_NAME=alphagate_auth
AUTH_SECURE_COOKIES=false
AUTH_TRUST_HOST=true

## Apps (dev ports)
MAIN_APP_PORT=3001
DASHBOARD_PORT=3000

## Dashboard (Console)
# Main app URL for server actions and fetches
NEXT_PUBLIC_MAIN_APP_URL=http://localhost:3001
# Default org id to use in the minimal console (dev only)
NEXT_PUBLIC_DEFAULT_ORG_ID=
